# Portfolio Frontend

Static frontend for the animated portfolio.

## Setup

```bash
npm install
npm start
```

Open `http://localhost:3001`.

The contact form sends requests to `http://localhost:5000/api/contact`.
