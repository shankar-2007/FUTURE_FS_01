# Portfolio Database

SQLite database repo for the portfolio backend.

The backend creates the local database automatically at:

```text
database/data/portfolio.sqlite
```

The schema is documented in `schema.sql`.

## Local Use

Start the backend from the `backend` folder. It will connect to this database path:

```text
../database/data/portfolio.sqlite
```

No separate database server is required for local development.
